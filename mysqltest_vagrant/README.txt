MYSQL Server VM

This folder is a Vagrant folder to spinup the MYSQL server VM with Jobo tables, configuration and users. Just copy all of the folder contents and "vagrant up".

Prerequisites:
- Vagrant installed
- Virtualbox installed
- Gow installed
- HeidiSQL installed or any SQL client(for Querying)